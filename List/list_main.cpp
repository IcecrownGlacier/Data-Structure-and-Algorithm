#include "list.h"

/*
int main()
{
	
	List list;
	ListNode *head = list.creat_list_node(0);
	list.insert(head, 1);
	list.insert(head, 2);
	list.insert(head, 3);
	list.insert(head, 4);
	list.print_list(head);

	
	vector<int> arraryList;
	arraryList = list.print_list_from_tail_to_head(head);

	for (int i = 0; i < arraryList.size();i++)
	{
	cout << arraryList.at(i)<< endl;
	}
	

	
	list.print_list(head);
	ListNode* newHead = list.reserver_without_head(head);
	list.print_list(newHead);

	
	
	list.print_list(head);
	ListNode *k = list.find_kth_to_tail(head, 3);
	list.print_list(k);
	

	ListNode *head1 = list.creat_list_node(0);
	list.insert(head1, 3);
	list.insert(head1, 3);
	list.insert(head1, 5);
	list.print_list(head1);
	ListNode *merge = list.merge(head, head1);
	list.print_list(merge);

	system("pause");
	return 0;
}

*/